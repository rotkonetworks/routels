router id 10.0.0.1;

protocol device {
}

protocol kernel {
    ipv4 { export all; };
}

protocol static {
    ipv4;
    route 10.0.0.0/24 via "eth0";
}

protocol bgp uplink {
    local as 65001;
    neighbor 10.0.0.2 as 65002;
    ipv4 { import all; export all; };
}
